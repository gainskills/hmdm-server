<?xml version="1.0" encoding="UTF-8"?>

<!--
  ~ Headwind MDM: Open Source Android MDM Software https://h-mdm.com
  ~
  ~ Copyright (C) 2019 Headwind Solutions LLC (https://h-mdm.com)
  ~
  ~ Licensed under the Apache License, Version 2.0 (the "License");
  ~ you may not use this file except in compliance with the License.
  ~ You may obtain a copy of the License at http://www.apache.org/licenses/LICENSE-2.0
  ~
  ~ Unless required by applicable law or agreed to in writing, software
  ~ distributed under the License is distributed on an "AS IS" BASIS,
  ~ WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  ~ See the License for the specific language governing permissions and
  ~ limitations under the License.
  -->

<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd"
        logicalFilePath="audit.changelog.xml">

    <!--
    The change set IDs MUST start with "plugin-<plugin identifier>" followed by dash symbol ALWAYS.
    The database object names declared by plugin MUST always start with "plugin_<plugin identifier>" followed by underscore
    symbol.
    -->

    <changeSet id="plugin-audit-04.10.2019-16:38" author="isv" context="common">
        <comment>Register audit plugin</comment>
        <sql>
            INSERT INTO plugins (
            identifier, name, description,
            javascriptModuleFile,
            functionsViewTemplate,
            settingsViewTemplate,
            namelocalizationkey,
            settingsPermission, functionsPermission, deviceFunctionsPermission
            )
            VALUES (
            'audit', 'Аудит', 'Аудит действий пользователей приложания',
            'app/components/plugins/audit/audit.module.js',
            'app/components/plugins/audit/views/audit.html',
            null,
            'plugin.audit.localization.key.name',
            'plugin_audit_access',
            'plugin_audit_access',
            'plugin_audit_access'
            );
        </sql>
        <rollback>
            DELETE FROM plugins WHERE identifier = 'audit';
        </rollback>
    </changeSet>

    <changeSet id="plugin-audit-04.10.2019-16:40" author="isv" context="common">
        <comment>Permissions for audit plugin access</comment>
        <sql>
            INSERT INTO permissions (name, description) VALUES ('plugin_audit_access', 'Имеет доступ к аудиту действий пользователей');
            INSERT INTO userRolePermissions (roleId, permissionId)
            SELECT id, currval('permissions_id_seq') FROM userroles WHERE name IN ('Администратор', 'Супер-Администратор');
        </sql>
        <rollback>
            DELETE FROM permissions WHERE name = 'plugin_audit_access';
        </rollback>
    </changeSet>

    <changeSet id="plugin-audit-04.10.2019-16:42" author="isv" context="common">
        <comment>Table,new: plugin_audit_log</comment>
        <sql>
            CREATE TABLE plugin_audit_log (
                id serial NOT NULL CONSTRAINT plugin_audit_log_pr_key PRIMARY KEY,
                createTime BIGINT NOT NULL DEFAULT EXTRACT(EPOCH FROM NOW()) * 1000,
                customerId INT,
                userId INT,
                login VARCHAR(100),
                action VARCHAR(100),
                payload TEXT,
                ipAddress VARCHAR(500)
            );
        </sql>
        <rollback>
            DROP TABLE plugin_audit_log;
        </rollback>
    </changeSet>

    <changeSet id="plugin-audit-23.02.2020-16:42" author="seva" context="common">
        <comment>Column,new: errorCode</comment>
        <sql>
            ALTER TABLE plugin_audit_log ADD COLUMN errorCode INT DEFAULT 0;
        </sql>
        <rollback>
            ALTER TABLE plugin_audit_log DROP COLUMN errorCode;
        </rollback>
    </changeSet>

    <changeSet id="plugin-audit-22.05.2020-12:47" author="seva" context="common">
        <comment>Fix user role permissions assuming admin and super-admin have fixed ids</comment>
        <sql>
            DELETE FROM userRolePermissions WHERE roleId=1 AND permissionId=(SELECT id FROM permissions WHERE name='plugin_audit_access');
            INSERT INTO userRolePermissions VALUES (1, (SELECT id FROM permissions WHERE name='plugin_audit_access'));
            DELETE FROM userRolePermissions WHERE roleId=2 AND permissionId=(SELECT id FROM permissions WHERE name='plugin_audit_access');
            INSERT INTO userRolePermissions VALUES (2, (SELECT id FROM permissions WHERE name='plugin_audit_access'));

        </sql>
        <rollback>
            DELETE FROM userRolePermissions WHERE roleId=1 AND permissionId=(SELECT id FROM permissions WHERE name='plugin_audit_access');
            DELETE FROM userRolePermissions WHERE roleId=2 AND permissionId=(SELECT id FROM permissions WHERE name='plugin_audit_access');
        </rollback>
    </changeSet>


    <!-- ===================================================================
         Index plugin_audit_log_customerid_createtime_idx on plugin_audit_log (customerId, createTime)

         Three changesets, not one. These indexes were first authored as
         'convergent'; re-authoring them as 'hmdm' changes the Liquibase
         identity (id, author, filename), so a database that already ran the
         convergent changesets treats these as new. The cleanup drops an index
         left unusable by an interrupted CONCURRENTLY build, the guard reports
         a same-name index with a different definition, and the create runs
         only when the name is genuinely absent.

         CREATE INDEX CONCURRENTLY IF NOT EXISTS is deliberately NOT used: it
         silently accepts an existing invalid index, and Liquibase then records
         the changeset as successful.

         Identifier case is load-bearing. The DDL is unquoted, so PostgreSQL
         folds these names to lower case; every catalog comparison below uses
         the lower-cased form and is schema-qualified.
         =================================================================== -->

    <changeSet id="plugin-audit-2026.06.17-idx-cust-time-cleanup" author="hmdm"
               context="common" runInTransaction="false">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="1">
                SELECT count(*) FROM pg_class idx
                JOIN pg_namespace n ON n.oid = idx.relnamespace
                JOIN pg_index i    ON i.indexrelid = idx.oid
                WHERE n.nspname = current_schema()
                  AND idx.relname = 'plugin_audit_log_customerid_createtime_idx'
                  AND (NOT i.indisvalid OR NOT i.indisready)
            </sqlCheck>
        </preConditions>
        <comment>Drop plugin_audit_log_customerid_createtime_idx only if it was left invalid or not ready</comment>
        <sql>DROP INDEX CONCURRENTLY IF EXISTS plugin_audit_log_customerid_createtime_idx;</sql>
    </changeSet>

    <changeSet id="plugin-audit-2026.06.17-idx-cust-time-guard" author="hmdm" context="common">
        <preConditions onFail="WARN"
            onFailMessage="Index plugin_audit_log_customerid_createtime_idx exists in the current schema with
                           an unexpected definition (unique, partial, expression key, INCLUDE columns,
                           non-btree, non-default sort order or operator class, or a different
                           table/columns). It has NOT been modified and the intended index has NOT been
                           created; queries on plugin_audit_log (customerId, createTime) remain unindexed.
                           Resolve manually, then re-run the verification query.">
            <sqlCheck expectedResult="0">
                SELECT count(*) FROM pg_class idx
                JOIN pg_namespace n  ON n.oid = idx.relnamespace
                JOIN pg_index i      ON i.indexrelid = idx.oid
                JOIN pg_class tbl    ON tbl.oid = i.indrelid
                JOIN pg_am am        ON am.oid = idx.relam
                WHERE n.nspname = current_schema()
                  AND idx.relname = 'plugin_audit_log_customerid_createtime_idx'
                  AND i.indisvalid AND i.indisready
                  AND NOT (
                        tbl.relname      = 'plugin_audit_log'
                    AND i.indisunique    = false
                    AND i.indisexclusion = false
                    AND i.indislive      = true
                    AND i.indpred        IS NULL
                    AND i.indexprs       IS NULL
                    AND am.amname        = 'btree'
                    AND i.indnkeyatts    = 2
                    AND i.indnatts       = 2
                    AND (SELECT array_agg(a.attname ORDER BY k.ord)
                         FROM unnest(i.indkey) WITH ORDINALITY AS k(attnum, ord)
                         LEFT JOIN pg_attribute a
                                ON a.attrelid = i.indrelid AND a.attnum = k.attnum)
                        = ARRAY['customerid', 'createtime']
                    AND (SELECT bool_and(o = 0) FROM unnest(i.indoption) AS o)
                    AND (SELECT bool_and(oc.opcdefault)
                         FROM unnest(i.indclass) AS c JOIN pg_opclass oc ON oc.oid = c)
                  )
            </sqlCheck>
        </preConditions>
        <comment>Report a valid but differently-defined plugin_audit_log_customerid_createtime_idx, without touching it</comment>
        <sql>SELECT 1;</sql>
    </changeSet>

    <changeSet id="plugin-audit-2026.06.17-idx-cust-time" author="hmdm"
               context="common" runInTransaction="false">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">
                SELECT count(*) FROM pg_class idx
                JOIN pg_namespace n ON n.oid = idx.relnamespace
                WHERE n.nspname = current_schema()
                  AND idx.relname = 'plugin_audit_log_customerid_createtime_idx'
            </sqlCheck>
        </preConditions>
        <comment>Index,new: plugin_audit_log_customerid_createtime_idx on plugin_audit_log (customerId, createTime)</comment>
        <sql>CREATE INDEX CONCURRENTLY plugin_audit_log_customerid_createtime_idx
             ON plugin_audit_log (customerId, createTime);</sql>
        <rollback>DROP INDEX CONCURRENTLY IF EXISTS plugin_audit_log_customerid_createtime_idx;</rollback>
    </changeSet>

</databaseChangeLog>