<?xml version="1.0" encoding="UTF-8"?>

<!--
  ~ Headwind MDM: Open Source Android MDM Software https://h-mdm.com
  ~
  ~ Copyright (C) 2019 Headwind Solutions LLC (https://h-mdm.com)
  ~
  ~ Licensed under the Apache License, Version 2.0 (the "License");
  ~ you may not use this file except in compliance with the License.
  ~ You may obtain a copy of the License at http://www.apache.org/licenses/LICENSE-2.0
  ~
  ~ Unless required by applicable law or agreed to in writing, software
  ~ distributed under the License is distributed on an "AS IS" BASIS,
  ~ WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  ~ See the License for the specific language governing permissions and
  ~ limitations under the License.
  -->

<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd"
        logicalFilePath="deviceinfo.changelog.xml">

    <!--
    The change set IDs MUST start with "plugin-<plugin identifier>" followed by dash symbol ALWAYS.
    The database object names declared by plugin MUST always start with "plugin_<plugin identifier>" followed by underscore
    symbol.
    -->

    <changeSet id="plugin-deviceinfo-22.10.2019-13:34" author="isv" context="common">
        <comment>Plugin,new: deviceinfo</comment>
        <sql>
            INSERT INTO plugins (
            identifier, name, description,
            javascriptModuleFile,
            functionsViewTemplate,
            settingsViewTemplate,
            namelocalizationkey,
            settingsPermission, functionsPermission, deviceFunctionsPermission,
            enabledForDevice
            )
            VALUES (
            'deviceinfo', 'Детальная информация', 'Детальная и динамическая информация об устройствах',
            'app/components/plugins/deviceinfo/deviceinfo.module.js',
            'app/components/plugins/deviceinfo/views/info.html',
            'app/components/plugins/deviceinfo/views/settings.html',
            'plugin.deviceinfo.localization.key.name',
            'plugin_deviceinfo_access',
            'plugin_deviceinfo_access',
            'plugin_deviceinfo_access',
            TRUE
            );
        </sql>
        <rollback>
            DELETE FROM plugins WHERE identifier = 'deviceinfo';
        </rollback>
    </changeSet>

    <changeSet id="plugin-deviceinfo-22.10.2019-13:36" author="isv" context="common">
        <comment>Permission,new: plugin_deviceinfo_access</comment>
        <sql>
            INSERT INTO permissions (name, description) VALUES ('plugin_deviceinfo_access', 'Имеет доступ к детальной и динамической информации об устройствах');
            INSERT INTO userRolePermissions (roleId, permissionId)
                SELECT id, currval('permissions_id_seq')
                FROM userroles;
        </sql>
        <rollback>
            DELETE FROM permissions WHERE name = 'plugin_deviceinfo_access';
        </rollback>
    </changeSet>

    <changeSet id="plugin-deviceinfo-22.10.2019-13:38" author="isv" context="common">
        <comment>Table,new: plugin_deviceinfo_settings</comment>
        <sql>
            CREATE TABLE plugin_deviceinfo_settings (
                id serial NOT NULL CONSTRAINT plugin_deviceinfo_settings_pr_key PRIMARY KEY,
                customerId INT NOT NULL REFERENCES customers (id) ON DELETE CASCADE,
                dataPreservePeriod INT NOT NULL DEFAULT 30
            );

            ALTER TABLE plugin_deviceinfo_settings ADD CONSTRAINT plugin_deviceinfo_settings_customer_unique UNIQUE (customerId);
        </sql>
        <rollback>
            ALTER TABLE plugin_deviceinfo_settings DROP CONSTRAINT plugin_deviceinfo_settings_customer_unique;
            DROP TABLE plugin_deviceinfo_settings;
        </rollback>
    </changeSet>

    <changeSet id="plugin-deviceinfo-22.10.2019-15:28" author="isv" context="common">
        <comment>Table,new: plugin_deviceinfo_deviceParams</comment>
        <sql>
            CREATE TABLE plugin_deviceinfo_deviceParams (
                id serial NOT NULL CONSTRAINT plugin_deviceinfo_deviceParams_pr_key PRIMARY KEY,
                deviceId INT NOT NULL REFERENCES devices (id) ON DELETE CASCADE,
                customerId INT NOT NULL REFERENCES customers (id) ON DELETE CASCADE,
                ts BIGINT NOT NULL
            );
        </sql>
        <rollback>
            DROP TABLE plugin_deviceinfo_deviceParams;
        </rollback>
    </changeSet>

    <changeSet id="plugin-deviceinfo-22.10.2019-15:31" author="isv" context="common">
        <comment>Table,new: plugin_deviceinfo_deviceParams_device</comment>
        <sql>
            CREATE TABLE plugin_deviceinfo_deviceParams_device (
                id serial NOT NULL CONSTRAINT plugin_deviceinfo_deviceParams_device_pr_key PRIMARY KEY,
                recordId INT NOT NULL UNIQUE REFERENCES plugin_deviceinfo_deviceParams (id) ON DELETE CASCADE,
                batteryLevel INT,
                batteryCharging VARCHAR(20),
                ip VARCHAR(50),
                keyguard BOOLEAN,
                ringVolume INT,
                wifi BOOLEAN,
                mobileData BOOLEAN,
                gps BOOLEAN,
                bluetooth BOOLEAN
            );
        </sql>
        <rollback>
            DROP TABLE plugin_deviceinfo_deviceParams_device;
        </rollback>
    </changeSet>

    <changeSet id="plugin-deviceinfo-22.10.2019-16:02" author="isv" context="common">
        <comment>Table,new: plugin_deviceinfo_deviceParams_wifi</comment>
        <sql>
            CREATE TABLE plugin_deviceinfo_deviceParams_wifi (
                id serial NOT NULL CONSTRAINT plugin_deviceinfo_deviceParams_wifi_pr_key PRIMARY KEY,
                recordId INT NOT NULL UNIQUE REFERENCES plugin_deviceinfo_deviceParams (id) ON DELETE CASCADE,
                rssi INT,
                ssid VARCHAR(500),
                security VARCHAR(500),
                state VARCHAR(20),
                ip VARCHAR(50),
                tx BIGINT,
                rx BIGINT
            );
        </sql>
        <rollback>
            DROP TABLE plugin_deviceinfo_deviceParams_wifi;
        </rollback>
    </changeSet>

    <changeSet id="plugin-deviceinfo-22.10.2019-16:29" author="isv" context="common">
        <comment>Table,new: plugin_deviceinfo_deviceParams_gps</comment>
        <sql>
            CREATE TABLE plugin_deviceinfo_deviceParams_gps (
                id serial NOT NULL CONSTRAINT plugin_deviceinfo_deviceParams_gps_pr_key PRIMARY KEY,
                recordId INT NOT NULL UNIQUE REFERENCES plugin_deviceinfo_deviceParams (id) ON DELETE CASCADE,
                state VARCHAR(20),
                lat DOUBLE PRECISION,
                lon DOUBLE PRECISION,
                alt DOUBLE PRECISION,
                speed DOUBLE PRECISION,
                course DOUBLE PRECISION
            );
        </sql>
        <rollback>
            DROP TABLE plugin_deviceinfo_deviceParams_gps;
        </rollback>
    </changeSet>

    <changeSet id="plugin-deviceinfo-22.10.2019-16:48" author="isv" context="common">
        <comment>Table,new: plugin_deviceinfo_deviceParams_mobile</comment>
        <sql>
            CREATE TABLE plugin_deviceinfo_deviceParams_mobile (
                id serial NOT NULL CONSTRAINT plugin_deviceinfo_deviceParams_mobile_pr_key PRIMARY KEY,
                recordId INT NOT NULL UNIQUE REFERENCES plugin_deviceinfo_deviceParams (id) ON DELETE CASCADE,
                rssi INT,
                carrier VARCHAR(50),
                data BOOLEAN,
                ip VARCHAR(50),
                state VARCHAR(20),
                simState VARCHAR(20),
                tx BIGINT,
                rx BIGINT
            );
        </sql>
        <rollback>
            DROP TABLE plugin_deviceinfo_deviceParams_mobile;
        </rollback>
    </changeSet>

    <changeSet id="plugin-deviceinfo-22.10.2019-16:49" author="isv" context="common">
        <comment>Table,new: plugin_deviceinfo_deviceParams_mobile2</comment>
        <sql>
            CREATE TABLE plugin_deviceinfo_deviceParams_mobile2 (
                id serial NOT NULL CONSTRAINT plugin_deviceinfo_deviceParams_mobile2_pr_key PRIMARY KEY,
                recordId INT NOT NULL UNIQUE REFERENCES plugin_deviceinfo_deviceParams (id) ON DELETE CASCADE,
                rssi INT,
                carrier VARCHAR(50),
                data BOOLEAN,
                ip VARCHAR(50),
                state VARCHAR(20),
                simState VARCHAR(20),
                tx BIGINT,
                rx BIGINT
            );
        </sql>
        <rollback>
            DROP TABLE plugin_deviceinfo_deviceParams_mobile2;
        </rollback>
    </changeSet>

    <changeSet id="plugin-deviceinfo-29.10.2019-14:03" author="isv" context="common">
        <comment>Columns,new: plugin_deviceinfo_settings#sendData,intervalMins</comment>
        <sql>
            ALTER TABLE plugin_deviceinfo_settings ADD COLUMN sendData BOOLEAN NOT NULL DEFAULT FALSE;
            ALTER TABLE plugin_deviceinfo_settings ADD COLUMN intervalMins INT NOT NULL DEFAULT 15;
        </sql>
        <rollback>
            ALTER TABLE plugin_deviceinfo_settings DROP COLUMN intervalMins;
            ALTER TABLE plugin_deviceinfo_settings DROP COLUMN sendData;
        </rollback>
    </changeSet>

    <changeSet id="plugin-deviceinfo-26.01.2021-13:23" author="seva" context="common">
        <comment>Columns,new: plugin_deviceinfo_deviceparams_device#usbStorage,memoryTotal,memoryAvailable</comment>
        <sql>
            ALTER TABLE plugin_deviceinfo_deviceparams_device ADD COLUMN usbStorage BOOLEAN;
            ALTER TABLE plugin_deviceinfo_deviceparams_device ADD COLUMN memoryTotal INT;
            ALTER TABLE plugin_deviceinfo_deviceparams_device ADD COLUMN memoryAvailable INT;
        </sql>
        <rollback>
            ALTER TABLE plugin_deviceinfo_deviceparams_device DROP COLUMN usbStorage;
            ALTER TABLE plugin_deviceinfo_deviceparams_device DROP COLUMN memoryTotal;
            ALTER TABLE plugin_deviceinfo_deviceparams_device DROP COLUMN memoryAvailable;
        </rollback>
    </changeSet>

    <!-- ===================================================================
         Index plugin_deviceinfo_deviceparams_deviceid_ts_idx on plugin_deviceinfo_deviceParams (deviceId, ts)

         Three changesets, not one. These indexes were first authored as
         'convergent'; re-authoring them as 'hmdm' changes the Liquibase
         identity (id, author, filename), so a database that already ran the
         convergent changesets treats these as new. The cleanup drops an index
         left unusable by an interrupted CONCURRENTLY build, the guard reports
         a same-name index with a different definition, and the create runs
         only when the name is genuinely absent.

         CREATE INDEX CONCURRENTLY IF NOT EXISTS is deliberately NOT used: it
         silently accepts an existing invalid index, and Liquibase then records
         the changeset as successful.

         Identifier case is load-bearing. The DDL is unquoted, so PostgreSQL
         folds these names to lower case; every catalog comparison below uses
         the lower-cased form and is schema-qualified.
         =================================================================== -->

    <changeSet id="plugin-deviceinfo-2026.06.17-idx-device-ts-cleanup" author="hmdm"
               context="common" runInTransaction="false">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="1">
                SELECT count(*) FROM pg_class idx
                JOIN pg_namespace n ON n.oid = idx.relnamespace
                JOIN pg_index i    ON i.indexrelid = idx.oid
                WHERE n.nspname = current_schema()
                  AND idx.relname = 'plugin_deviceinfo_deviceparams_deviceid_ts_idx'
                  AND (NOT i.indisvalid OR NOT i.indisready)
            </sqlCheck>
        </preConditions>
        <comment>Drop plugin_deviceinfo_deviceparams_deviceid_ts_idx only if it was left invalid or not ready</comment>
        <sql>DROP INDEX CONCURRENTLY IF EXISTS plugin_deviceinfo_deviceparams_deviceid_ts_idx;</sql>
    </changeSet>

    <changeSet id="plugin-deviceinfo-2026.06.17-idx-device-ts-guard" author="hmdm" context="common">
        <preConditions onFail="WARN"
            onFailMessage="Index plugin_deviceinfo_deviceparams_deviceid_ts_idx exists in the current schema with
                           an unexpected definition (unique, partial, expression key, INCLUDE columns,
                           non-btree, non-default sort order or operator class, or a different
                           table/columns). It has NOT been modified and the intended index has NOT been
                           created; queries on plugin_deviceinfo_deviceParams (deviceId, ts) remain unindexed.
                           Resolve manually, then re-run the verification query.">
            <sqlCheck expectedResult="0">
                SELECT count(*) FROM pg_class idx
                JOIN pg_namespace n  ON n.oid = idx.relnamespace
                JOIN pg_index i      ON i.indexrelid = idx.oid
                JOIN pg_class tbl    ON tbl.oid = i.indrelid
                JOIN pg_am am        ON am.oid = idx.relam
                WHERE n.nspname = current_schema()
                  AND idx.relname = 'plugin_deviceinfo_deviceparams_deviceid_ts_idx'
                  AND i.indisvalid AND i.indisready
                  AND NOT (
                        tbl.relname      = 'plugin_deviceinfo_deviceparams'
                    AND i.indisunique    = false
                    AND i.indisexclusion = false
                    AND i.indislive      = true
                    AND i.indpred        IS NULL
                    AND i.indexprs       IS NULL
                    AND am.amname        = 'btree'
                    AND i.indnkeyatts    = 2
                    AND i.indnatts       = 2
                    AND (SELECT array_agg(a.attname ORDER BY k.ord)
                         FROM unnest(i.indkey) WITH ORDINALITY AS k(attnum, ord)
                         LEFT JOIN pg_attribute a
                                ON a.attrelid = i.indrelid AND a.attnum = k.attnum)
                        = ARRAY['deviceid', 'ts']
                    AND (SELECT bool_and(o = 0) FROM unnest(i.indoption) AS o)
                    AND (SELECT bool_and(oc.opcdefault)
                         FROM unnest(i.indclass) AS c JOIN pg_opclass oc ON oc.oid = c)
                  )
            </sqlCheck>
        </preConditions>
        <comment>Report a valid but differently-defined plugin_deviceinfo_deviceparams_deviceid_ts_idx, without touching it</comment>
        <sql>SELECT 1;</sql>
    </changeSet>

    <changeSet id="plugin-deviceinfo-2026.06.17-idx-device-ts" author="hmdm"
               context="common" runInTransaction="false">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">
                SELECT count(*) FROM pg_class idx
                JOIN pg_namespace n ON n.oid = idx.relnamespace
                WHERE n.nspname = current_schema()
                  AND idx.relname = 'plugin_deviceinfo_deviceparams_deviceid_ts_idx'
            </sqlCheck>
        </preConditions>
        <comment>Index,new: plugin_deviceinfo_deviceparams_deviceid_ts_idx on plugin_deviceinfo_deviceParams (deviceId, ts)</comment>
        <sql>CREATE INDEX CONCURRENTLY plugin_deviceinfo_deviceparams_deviceid_ts_idx
             ON plugin_deviceinfo_deviceParams (deviceId, ts);</sql>
        <rollback>DROP INDEX CONCURRENTLY IF EXISTS plugin_deviceinfo_deviceparams_deviceid_ts_idx;</rollback>
    </changeSet>

    <!-- ===================================================================
         Index plugin_deviceinfo_deviceparams_customerid_ts_idx on plugin_deviceinfo_deviceParams (customerId, ts)

         Three changesets, not one. These indexes were first authored as
         'convergent'; re-authoring them as 'hmdm' changes the Liquibase
         identity (id, author, filename), so a database that already ran the
         convergent changesets treats these as new. The cleanup drops an index
         left unusable by an interrupted CONCURRENTLY build, the guard reports
         a same-name index with a different definition, and the create runs
         only when the name is genuinely absent.

         CREATE INDEX CONCURRENTLY IF NOT EXISTS is deliberately NOT used: it
         silently accepts an existing invalid index, and Liquibase then records
         the changeset as successful.

         Identifier case is load-bearing. The DDL is unquoted, so PostgreSQL
         folds these names to lower case; every catalog comparison below uses
         the lower-cased form and is schema-qualified.
         =================================================================== -->

    <changeSet id="plugin-deviceinfo-2026.06.17-idx-cust-ts-cleanup" author="hmdm"
               context="common" runInTransaction="false">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="1">
                SELECT count(*) FROM pg_class idx
                JOIN pg_namespace n ON n.oid = idx.relnamespace
                JOIN pg_index i    ON i.indexrelid = idx.oid
                WHERE n.nspname = current_schema()
                  AND idx.relname = 'plugin_deviceinfo_deviceparams_customerid_ts_idx'
                  AND (NOT i.indisvalid OR NOT i.indisready)
            </sqlCheck>
        </preConditions>
        <comment>Drop plugin_deviceinfo_deviceparams_customerid_ts_idx only if it was left invalid or not ready</comment>
        <sql>DROP INDEX CONCURRENTLY IF EXISTS plugin_deviceinfo_deviceparams_customerid_ts_idx;</sql>
    </changeSet>

    <changeSet id="plugin-deviceinfo-2026.06.17-idx-cust-ts-guard" author="hmdm" context="common">
        <preConditions onFail="WARN"
            onFailMessage="Index plugin_deviceinfo_deviceparams_customerid_ts_idx exists in the current schema with
                           an unexpected definition (unique, partial, expression key, INCLUDE columns,
                           non-btree, non-default sort order or operator class, or a different
                           table/columns). It has NOT been modified and the intended index has NOT been
                           created; queries on plugin_deviceinfo_deviceParams (customerId, ts) remain unindexed.
                           Resolve manually, then re-run the verification query.">
            <sqlCheck expectedResult="0">
                SELECT count(*) FROM pg_class idx
                JOIN pg_namespace n  ON n.oid = idx.relnamespace
                JOIN pg_index i      ON i.indexrelid = idx.oid
                JOIN pg_class tbl    ON tbl.oid = i.indrelid
                JOIN pg_am am        ON am.oid = idx.relam
                WHERE n.nspname = current_schema()
                  AND idx.relname = 'plugin_deviceinfo_deviceparams_customerid_ts_idx'
                  AND i.indisvalid AND i.indisready
                  AND NOT (
                        tbl.relname      = 'plugin_deviceinfo_deviceparams'
                    AND i.indisunique    = false
                    AND i.indisexclusion = false
                    AND i.indislive      = true
                    AND i.indpred        IS NULL
                    AND i.indexprs       IS NULL
                    AND am.amname        = 'btree'
                    AND i.indnkeyatts    = 2
                    AND i.indnatts       = 2
                    AND (SELECT array_agg(a.attname ORDER BY k.ord)
                         FROM unnest(i.indkey) WITH ORDINALITY AS k(attnum, ord)
                         LEFT JOIN pg_attribute a
                                ON a.attrelid = i.indrelid AND a.attnum = k.attnum)
                        = ARRAY['customerid', 'ts']
                    AND (SELECT bool_and(o = 0) FROM unnest(i.indoption) AS o)
                    AND (SELECT bool_and(oc.opcdefault)
                         FROM unnest(i.indclass) AS c JOIN pg_opclass oc ON oc.oid = c)
                  )
            </sqlCheck>
        </preConditions>
        <comment>Report a valid but differently-defined plugin_deviceinfo_deviceparams_customerid_ts_idx, without touching it</comment>
        <sql>SELECT 1;</sql>
    </changeSet>

    <changeSet id="plugin-deviceinfo-2026.06.17-idx-cust-ts" author="hmdm"
               context="common" runInTransaction="false">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">
                SELECT count(*) FROM pg_class idx
                JOIN pg_namespace n ON n.oid = idx.relnamespace
                WHERE n.nspname = current_schema()
                  AND idx.relname = 'plugin_deviceinfo_deviceparams_customerid_ts_idx'
            </sqlCheck>
        </preConditions>
        <comment>Index,new: plugin_deviceinfo_deviceparams_customerid_ts_idx on plugin_deviceinfo_deviceParams (customerId, ts)</comment>
        <sql>CREATE INDEX CONCURRENTLY plugin_deviceinfo_deviceparams_customerid_ts_idx
             ON plugin_deviceinfo_deviceParams (customerId, ts);</sql>
        <rollback>DROP INDEX CONCURRENTLY IF EXISTS plugin_deviceinfo_deviceparams_customerid_ts_idx;</rollback>
    </changeSet>

</databaseChangeLog>